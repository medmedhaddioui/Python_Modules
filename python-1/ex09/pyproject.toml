[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "function return how many times `target` appears in `lst`."
license = "MIT"
authors = [ { name = "mel-hadd", email = "mel-hadd@42.fr" } ]
urls = { "Home-page" = "https://github.com/mel-hadd/ft_package" }
